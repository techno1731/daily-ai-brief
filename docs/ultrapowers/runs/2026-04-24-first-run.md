# First run — 2026-04-24

**Routine id:** `trig_01NHsr6xHjvUb47L97AXudKp`
**Environment:** `env_012bqWG6PK9a55NtPYFjkros` (Default, anthropic_cloud kind)
**Trigger mode:** manual (run-now), not cron-driven
**Outcome:** ✅ full pipeline executed · ❌ delivery blocked at sandbox egress

## Summary

Every orchestrator step succeeded from config load through payload construction. The final HTTP POST to Resend was rejected by the `/schedule` sandbox's outbound host allowlist — not by Resend. Authorization header never left the sandbox; no credentials exposed. One retry after 5s returned the identical 403 "Host not in allowlist," confirming the deny is policy, not transient.

## Per-step status

| Step | Status | Notes |
|---|---|---|
| 1. WebFetch config.yaml + email.html | ✅ | raw.githubusercontent.com is allowlisted |
| 2. Compute window | ✅ | 24h window (Friday) |
| 3. Dispatch sub-agents (Task tool) | ✅ | **Task tool works inside CCR routines** — primary research unknown now resolved |
| 4. Collect + validate JSON | ✅ | 11/11 categories returned valid schema |
| 5. Synthesize TL;DR / deep research / patterns | ✅ | Opus orchestrator |
| 6. Render HTML from template | ✅ | Per-item substitutions clean |
| 7. Compute subject line | ✅ | Non-degraded branch |
| 8. POST to Resend | ❌ | **HTTP 403 "Host not in allowlist" — sandbox egress policy** |
| 9. Log summary | ✅ | Surfaced via the /schedule trace |

## Item counts (30 total across 11 categories)

| Category | Items |
|---|---:|
| model-releases | 2 |
| agents | 2 |
| rag | 3 |
| mcp | 2 |
| claude-skills-code | 5 |
| llm-gateways | 5 |
| evals | 1 |
| observability | 2 |
| self-hosting | 3 |
| ai-platform-devex | 2 |
| eu-ai-act | 3 |

No categories empty, no categories marked failed.

## Issues observed (all non-fatal to the pipeline, but affect content quality)

1. **`api.resend.com` not on sandbox outbound allowlist** — blocking delivery. This is the only show-stopper.
2. **Multiple publisher landing pages returned 403 to WebFetch** — `latent.space`, `hamel.dev`, `huyenchip.com`, `helicone.ai`, `changelog.langchain.com`, `anthropic.com/news`. Sub-agents backfilled via WebSearch; coverage held but degraded. Root cause likely the same sandbox networking profile (host bot-checks the WebFetch proxy's UA or IP).
3. **arXiv Atom API 403** in the rag sub-agent — papers sourced indirectly; a few items near the window boundary may be from Apr 22 rather than strictly ≥ Apr 23.
4. **GitHub REST API unauth rate limits hit** — expected with no `GITHUB_TOKEN` set; sub-agents fell back to WebFetch. Content quality still acceptable.
5. **`old.reddit.com` blocked** for self-hosting sub-agent. r/LocalLLaMA section missing; self-hosting coverage drawn from GitHub releases only.

## What this confirms vs the spec's open risks

From `2026-04-24-research-brief.md § Open risks`:

| Risk | Resolution |
|---|---|
| Task-tool parallelism inside /schedule not documentation-confirmed | **Resolved: works.** Sequential fallback unused. |
| /schedule max runtime undocumented | Not hit — runtime was comfortably within any plausible ceiling. |
| Gmail Promotions placement | Not yet tested — no email delivered. |
| Resend shared-sender (onboarding@resend.dev) cosmetic "via resend.dev" | N/A — already promoted to `brief@ultrapowers.dev` during deployment. |

## Follow-ups

### Blocking (must resolve before the routine is autonomous)

- **[P0] Unblock `api.resend.com` egress.** Options:
  - (a) Whitelist the host on `env_012bqWG6PK9a55NtPYFjkros` if the platform exposes an allowlist setting. Preferred if available.
  - (b) Deploy a thin proxy on a host that IS allowlisted (e.g., `*.vercel.app`) that accepts the routine's payload + a shared secret and forwards to Resend server-side. One function, one auth header, < 30 lines.
  - (c) Switch to an email provider whose API host happens to be allowlisted. Unlikely to be cleaner than (b).

### Non-blocking (improves quality / resilience)

- Add a fine-grained `GITHUB_TOKEN` to the routine prompt to raise GitHub REST rate limit from 60/hr to 5000/hr.
- Characterize the WebFetch 403 pattern — is it per-domain bot-blocking, or a systemic UA/IP block? If systemic, the list of affected publishers will grow as we add sources.
- Consider adding a `force_webfetch_useragent` instruction in sub-agent prompts, though WebFetch doesn't surface a UA knob.
- Spec's arXiv fetch strategy should add a fallback: if Atom API 403s, try `arxiv.org/list/cs.CL/new` HTML listing as a secondary path (listed in the original research but not currently in `fetch_strategy` for the rag category).

## Decision log

- Kept the routine's `sources` field empty (no git clone). Rationale: repo is public, `raw.githubusercontent.com` is allowlisted, so WebFetch of config and template works without requiring a GitHub App install.
- Resend key embedded in routine prompt rather than env var — CCR API does not expose an env_vars field as of 2026-04-24.
- No auto-retry on the egress failure — one retry confirmed it was policy, not transient; further retries would just waste trace quota.
